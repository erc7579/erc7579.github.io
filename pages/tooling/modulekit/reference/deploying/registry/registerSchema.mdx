> ⚠️ **Deprecation Notice**
>
> ModuleKit was developed by Rhinestone and is no longer actively supported. 
> For new projects, we recommend using the **Rhinestone SDK** instead.
>
> **→ [Rhinestone SDK Documentation](https://docs.rhinestone.dev)**

---
title: "registerSchema"
---

Register a schema on the Registry.

## Usage

```solidity
SchemaUID schemaUID = registerSchema({
    schema: schema,
    validator: validator
});
```

## Parameters

### schema

- Type: `string`

The schema string to register.

### validator

- Type: `address`

The address of the schema validator to register.

## Returns

### schemaUID

- Type: `SchemaUID | bytes32`

The schema UID.
